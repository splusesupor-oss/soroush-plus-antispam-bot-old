# پرامپت: افزودن قابلیت «کنترل سرگرمی به تفکیک گروه» به ربات

> این متن را عیناً به دستیار کدنویسِ خودت بده. برای رباتِ اسپلاس/تلگرامیِ پایتونی
> با معماری handler نوشته شده، ولی به‌اندازهٔ کافی عمومی هست که روی هر ربات مشابهی
> پیاده شود.

---

## نقش تو

تو یک مهندس نرم‌افزار پایتون هستی که روی یک ربات ضداسپم/سرگرمی کار می‌کند.
باید یک قابلیت جدید اضافه کنی: **روشن و خاموش کردن بازی‌های داخل ربات، جداگانه برای هر گروه.**

## قوانین سختگیرانه (نقض نکن)

1. **هیچ دادهٔ موجودی حذف یا تغییر نکن** — سکه‌ها، امتیازها، بازی‌های در جریان،
   تنظیمات گروه‌ها، فهرست ادمین‌ها و مالک. وضعیت جدید باید در **فایل مستقل خودش**
   ذخیره شود، نه داخل فایل‌های تنظیمات موجود.
2. **هیچ بازی، سیستم پاداش، storage، پنل ادمین یا قابلیت موجودی را دست نزن.**
   فقط گارد اضافه کن، منطق داخلی بازی‌ها را بازنویسی نکن.
3. **وقتی بازی مسدود است، مطلقاً هیچ اثری نباید بگذارد:** نه state ساخته شود،
   نه تایمر، نه سکه/جایزه‌ای پرداخت شود، نه شمارنده‌ای بالا برود. فقط یک پیام هشدار.
4. **Bold باید entity واقعی باشد**، نه ستارهٔ مارک‌داون (`**متن**`). یعنی
   `MessageEntityBold` با offset/length بر حسب **واحدهای UTF-16**.
5. **هیچ تستی را حذف، skip، xfail یا ضعیف نکن.** اگر تستی قرمز شد، علت واقعی را رفع کن.
6. اگر ربات چند instance دارد، کلید/فایل ذخیره‌سازی نباید بین instanceها تداخل کند.
7. سایت بازی / وب‌اپ بیرونی را دست نزن — این قابلیت فقط سرگرمی‌های **داخل خود ربات** است.

## قابلیت مورد نیاز

### دستورها
دو دستور متنی داخل گروه:
- `سرگرمی خاموش` → بازی‌های آن گروه غیرفعال می‌شود.
- `سرگرمی فعال` → دوباره فعال می‌شود.

**دسترسی:** فقط مالک اصلی ربات، مالک ثبت‌شدهٔ گروه، یا ادمین ثبت‌شدهٔ همان گروه.
(از همان تابع بررسی دسترسیِ موجود در پروژه استفاده کن، مثلاً `has_admin_permission`.)
کاربر عادی → پیام رد دسترسی.
در چت خصوصی → پیام «این دستور فقط داخل گروه کار می‌کند».

### پیام‌ها (متن‌ها را عیناً و بدون کم و زیاد کردن کاراکتر استفاده کن)

| موقعیت | متن | Bold |
|---|---|---|
| خاموش شد | `🎮 بازی های روباه غیر فعال شد` | کل پیام |
| فعال شد | `🍬 : بازی های روباه فعال شد میتوانید با دستور لیست بازی از بازی ها استفاده کنید` | کل پیام |
| تلاش برای بازی وقتی خاموش است | `بازی های روباه عموم غیر فعال می‌باشند ؛ برای فعال سازی باید ادمین یا مالک با دستور سرگرمی فعال بازی هارو فعال کند تا بتوانید از بازی ها استفاده کنید ☑️` | **فقط** عبارت `سرگرمی فعال` |
| رد دسترسی | `❌ فقط مالک یا ادمین‌های گروه اجازه تغییر وضعیت سرگرمی را دارند` | — |

### رفتار پیش‌فرض
هر گروهی که تنظیم نشده = **فعال**. اگر خواندن فایل ذخیره‌سازی به هر دلیلی خطا داد،
باز هم **فعال** برگردان — یک خطای دیسک نباید بازی‌ها را برای همیشه ببندد.

---

## پیاده‌سازی

### ۱. ماژول جدید `modules/entertainment_control.py`

این بخش‌ها را داشته باشد:

**ذخیره‌سازی**
- فایل مستقل `entertainment_mode.json` در پوشهٔ config رانتایم (برای هر instance جدا).
- نوشتن **اتمیک** (write به فایل موقت + rename)، تا قطعیِ برق فایل را خراب نکند.
- خواندن با **کش mtime**: اگر mtime تغییر نکرده، از کش بخوان.
- شناسهٔ گروه را قبل از استفاده به‌عنوان کلید **نرمال‌سازی** کن (همان تابع
  `normalize_group_id` پروژه)، تا `-100…` و شکل‌های دیگرِ یک گروه دو رکورد نسازند.
- توابع: `load()`, `save(data)`, `is_enabled(chat_id)`, `set_enabled(chat_id, enabled)`,
  `enable(chat_id)`, `disable(chat_id)`, `reset(chat_id)`.
  `reset` کلید گروه را پاک می‌کند تا به پیش‌فرض برگردد.

**Bold واقعی**
```python
def u16_length(text):
    # طول بر حسب واحد UTF-16 — همان چیزی که entity لازم دارد
    return len(str(text).encode("utf-16-le")) // 2

def full_bold_span(text):
    return 0, u16_length(text)

def blocked_bold_span(text=None):
    # (offset, length) عبارت «سرگرمی فعال» داخل پیام مسدودی
    text = BLOCKED_NOTICE if text is None else text
    i = text.find("سرگرمی فعال")
    if i < 0:
        return None
    return u16_length(text[:i]), u16_length("سرگرمی فعال")
```
> چرا UTF-16؟ چون ایموجی‌ها و برخی نویسه‌ها در پایتون یک کاراکتر ولی در پروتکل
> دو واحد حساب می‌شوند؛ با `len()` ساده، Bold روی متن جابه‌جا می‌افتد.

ارسال پیام با `formatting_entities=[MessageEntityBold(offset=…, length=…)]` و
اگر کلاینت `TypeError` داد (entity پشتیبانی نشد)، به متن ساده fallback کن.
import کتابخانهٔ کلاینت را در `try/except ImportError` بگذار و یک کلاس جایگزین
ساده تعریف کن، تا تست‌ها بدون نصب کتابخانه هم اجرا شوند.

**فهرست دستورهای بازی + نرمال‌سازی**
یک `frozenset` از تمام دستورهایی که بازیِ داخلی را **اجرا** می‌کنند بساز
(مثل: چیستان، حدس ایموجی، حدس پرچم، اسم فامیل، جای خالی، دروغ یا حقیقت، جک،
معما، بقا، مین یاب، نبرد، کارگاه، شرکت، خون آشام و …).

تابع `normalize(text)` بنویس که:
- نیم‌فاصله (`\u200c`) را به فاصله تبدیل کند،
- کاراکترهای جهت‌دهی (`\u200f`, `\u200e`) را حذف کند،
- «ي» عربی → «ی» فارسی و «ك» عربی → «ک» فارسی،
- فاصله‌های تکراری را یکی کند.

تا «چهار گزینه‌ای» و «چهار گزینه ای» و «حدس ايموجي» همگی درست تشخیص داده شوند.

**گارد**
```python
def blocks(chat_id, text):
    """گارد خالص، بدون I/O شبکه — برای تست آسان."""
    return is_game_command(text) and not is_enabled(chat_id)

async def guard(event, chat_id, text):
    """True یعنی بازی نباید اجرا شود."""
    if not blocks(chat_id, text):
        return False
    await send_blocked_notice(event)
    return True
```
جدا کردن `blocks` (خالص) از `guard` (async) مهم است: تست‌ها می‌توانند منطق را
بدون ساختن event قلابی بررسی کنند.

### ۲. اتصال در هندلر اصلی پیام

در `handlers/message_handler.py` (یا معادلش)، **قبل از** مسیرهای بازی:

```python
# ۱) شاخهٔ توگل
if clean_text in entertainment_control.COMMANDS:
    logger.log_info(f"HANDLER CALLED handler=entertainment_control command={clean_text!r} chat_id={chat_id}")
    if event.is_private:
        await event.reply("❌ این دستور فقط داخل گروه کار می‌کند.")
        return
    if not admin_tools.has_admin_permission(chat_id, user_id, getattr(sender, "username", None)):
        await event.reply(entertainment_control.PERMISSION_DENIED)
        return
    if clean_text == entertainment_control.COMMAND_DISABLE:
        entertainment_control.disable(chat_id)
        await entertainment_control.send_disabled_notice(event)
    else:
        entertainment_control.enable(chat_id)
        await entertainment_control.send_enabled_notice(event)
    return

# ۲) گارد مرکزی — هر بازی داخلی از همین یک نقطه رد می‌شود
if await entertainment_control.guard(event, chat_id, clean_text):
    logger.log_info(f"ENTERTAINMENT BLOCKED chat_id={chat_id} command={clean_text!r}")
    return
```

**نکتهٔ مهم:** دستور «لیست بازی» عمداً گارد **نمی‌شود** و همیشه کار می‌کند،
چون پیام فعال‌سازی خودش کاربر را به آن ارجاع می‌دهد.

### ۳. گارد لایهٔ دوم در روتر بازی‌ها

اگر پروژه روتر جداگانه‌ای برای بازی‌ها دارد (مثل `handlers/fox_games_router.py`)،
همان گارد را **داخل شاخهٔ شروع بازی** هم بگذار:

```python
if command in start_games:
    if await entertainment_control.guard(event, chat_id, text):
        log(logger, f"FOX GAME BLOCKED chat_id={chat_id} command={command!r}")
        return True
    ...  # ادامهٔ منطق موجود
```

دو لایه لازم است: اگر روتر روزی از مسیری غیر از هندلر اصلی صدا زده شود،
بازیِ خاموش باز هم اجرا نمی‌شود. گارد را **قبل از** هر ساخت state یا تایمری بگذار.

---

## تست‌ها

یک فایل تست جدید بنویس (مثلاً `tests/test_entertainment_control.py`) که این موارد را پوشش دهد:

- پیش‌فرض هر گروه فعال است.
- `disable` / `enable` / `reset` درست کار می‌کنند و **بین گروه‌ها ایزوله‌اند**
  (خاموش کردن گروه A روی گروه B اثر نگذارد).
- ماندگاری روی دیسک: بعد از پاک کردن کش، وضعیت درست خوانده شود.
- فایل خراب یا غایب → پیش‌فرض فعال، بدون exception.
- شناسه‌های مختلفِ یک گروه به یک کلید نرمال شوند.
- offset/length مربوط به Bold **دقیقاً** روی عبارت «سرگرمی فعال» بیفتد.
  این را با برش UTF-16 تأیید کن:
  ```python
  b = text.encode("utf-16-le")
  assert b[off*2:(off+ln)*2].decode("utf-16-le") == "سرگرمی فعال"
  ```
- Bold از نوع entity باشد، و متن هیچ `*` یا `**` نداشته باشد.
- نرمال‌سازی: «چهار گزینه‌ای» و «چهار گزینه ای» هر دو تشخیص داده شوند.
- وقتی خاموش است، هر دستور بازی مسدود شود؛ وقتی فعال است، هیچ‌کدام مسدود نشود.
- دستورهای غیربازی (پیام معمولی، دستور ادمین، «لیست بازی») هرگز مسدود نشوند.
- **مسدود شدن هیچ عارضه‌ای ندارد**: با یک event قلابی بررسی کن که پس از بلاک،
  هیچ state بازی ساخته نشده و هیچ سکه‌ای اضافه نشده.
- بدون دسترسی، وضعیت تغییر نکند.

### روش کار اجباری
1. اول قابلیت را بنویس و تست‌ها را سبز کن.
2. **عمداً یک باگ تزریق کن** (مثلاً `not is_enabled` را به `is_enabled` تغییر بده،
   یا offset بولد را ۱ واحد جابه‌جا کن) و **تأیید کن تست‌ها قرمز می‌شوند**.
   اگر قرمز نشدند، یعنی تست‌هایت واقعاً چیزی را نمی‌سنجند — تست را قوی‌تر کن.
3. باگ را رفع کن و تأیید کن دوباره سبز شدند.
4. **کل سوییت تست پروژه** را اجرا کن، نه فقط فایل جدید را.

---

## گزارش نهایی که باید بدهی

- فهرست فایل‌های اضافه‌شده و تغییر‌یافته.
- تعداد PASS/FAIL کل سوییت، قبل و بعد.
- تأیید اینکه باگ تزریقی واقعاً تست‌ها را قرمز کرد.
- تأیید اینکه هیچ داده، بازی، پاداش یا تنظیمات موجودی تغییر نکرده.
- هر تستی که هنوز قرمز است، با علت واقعی‌اش.
